const input = [
  ];

function part2() {
  answer = 0;
  inputs.forEach(x => {
    let red = 0;
    let green = 0;
    let blue = 0;
    
    //puzzle is still simple, let's just split on spaces
    let y = x.split(' ');
  });
}
